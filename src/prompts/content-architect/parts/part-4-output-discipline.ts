export function part4OutputDiscipline(): string {
  return `## Output discipline (JSON fields)
Return only the required JSON object — no markdown outside strings.

**title:** Curiosity-driven YouTube headline; psychology/mindset rooted; promises clarity not hacks.

**hook:** Exactly **2 sentences**, second person. Big Brother warmth + thesis undertone.

**thumbnailVisualDescription:** **4–8 sentences** of scene prose for Imagen (16:9 panel). Follow Official visual language + palette + character lock. One clear focal read; props and labels only when they teach the beat.

**thumbnailTextOverlay:** **2–6 words**, ALL CAPS, ASCII. Curiosity loop — tease payoff without spoiling.

**thumbnailTextGlow:** **cyan** for clarity / breakthrough energy; **amber** for warmth / restoration wins.

**pillar:** One enum from the five psychology sub-themes above.

Do not duplicate long art-direction blocks in short fields — the scene description carries the visual detail.`;
}
