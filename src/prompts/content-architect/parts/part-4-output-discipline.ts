export function part4OutputDiscipline(): string {
  return `## Output discipline (JSON fields)
Return only the required JSON object — no markdown outside strings.

**title:** Curiosity-driven YouTube headline; topic/concept rooted; promises clarity or structural insight.

**hook:** Exactly **2 sentences**, second person. Captures the host voice and active channel thesis/tone.

**thumbnailVisualDescription:** **4–8 sentences** of scene prose for Imagen (16:9 panel). Follow your active visual language, palette, and character specifications. Define a clear focal point and storyboard depth.

**thumbnailTextOverlay:** **2–6 words**, ALL CAPS, ASCII. Curiosity loop — tease payoff without spoiling.

**thumbnailTextGlow:** **cyan** for clarity/breakthrough highlight energy; **amber** for warmth/restoration highlight wins.

**pillar:** Custom lowercase content pillar keyword tailored to the topic (do not use generic numbering; write 1-3 lowercase words).

Do not duplicate long art-direction blocks in short fields — the scene description carries the visual detail.`;
}
