"use client";

/**
 * Audio stage actions live in {@link StudioEpisodeWatchShell}. This module is
 * kept for route compatibility with {@link StageAccessGate}.
 */
export function StudioAudioWorkspace(props: { videoId: string }) {
  void props;
  return null;
}
