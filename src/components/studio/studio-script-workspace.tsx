"use client";

/**
 * Script stage UI is rendered by {@link StudioEpisodeWatchShell} inside
 * {@link ScriptDraftProvider} (see `studio-episode-layout-client.tsx`).
 */
export function StudioScriptWorkspace(props: { videoId: string }) {
  void props;
  return null;
}
